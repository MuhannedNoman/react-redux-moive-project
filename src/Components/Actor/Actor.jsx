import React from "react";
import "./Actor.scss";

export default function Actor() {
  return (
    <div>
      This is the Actor Component
      <div>
        Display list of actors showing their name, and image same as movies
        page. Functionalities: Once you click on an actor you go to their own
        page. Use lazy loading functionality.
      </div>
    </div>
  );
}
