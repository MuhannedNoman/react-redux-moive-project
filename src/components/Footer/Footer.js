import { Col, Container, Row } from "react-bootstrap";

const Footer = () => {
  return (
    <footer className="bg-dark bg-gradient text-light py-4">
      <Container>Here is the footer</Container>
    </footer>
  );
};

export default Footer;
